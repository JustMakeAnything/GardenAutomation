switch:
  - platform: gpio
    pin: D1
    name: "Solar pump"
    id: solarpump
    restore_mode: ALWAYS_OFF

  - platform: gpio
    pin: D2
    name: "Buffer pump"
    id: bufferpump
    restore_mode: ALWAYS_OFF

  - platform: gpio
    pin: D5
    name: "EC Power"
    id: ecPower
    restore_mode: ALWAYS_OFF

